### TEMPLATE: quantifiers_negation (AUTHORITATIVE)

You must implement the Designer plan using explicit **quantifiers** and/or **negation**.

#### Required stem structure
- Present one main quantified statement, using words like:
  - “for all”, “for every”, “there exists”, “for some”, “at least one”
- The question must ask one of:
  - identify the correct negation
  - determine whether the statement is true/false under given constraints
  - select which reformulation is logically equivalent
- Keep maths content simple; the challenge is correct quantifier logic.

#### Option form
- Options should be different candidate statements (A–F), each a plausible negation/equivalent.
- Exactly one is correct.
- Avoid subtle wording ambiguity; make each option crisply distinct.

#### Distractor requirements
Include common quantifier mistakes:
- swapping ∀ and ∃ incorrectly
- negating inside/outside incorrectly (e.g. “not all” vs “all not”)
- forgetting to negate inequalities correctly
- negating “and/or” incorrectly

#### Constraint
Ensure all domains are explicitly stated (e.g. integers, reals, positive integers).
