# Schema Compression Prompt

You are given a schema that exceeds the bullet limit (max 4 bullets per section).

Your task: rewrite it to enforce the limits WITHOUT losing essential meaning.

## Original Schema

{schema_markdown}

## Requirements

- Seen in / context: MAX 4 bullets
- Possible wrong paths: MAX 4 bullets  
- Notes for generation: MAX 4 bullets
- Merge similar bullets if needed
- Keep the most important information
- Maintain actionable language
- Do NOT change the schema ID, prefix, title, or core move

Return ONLY the compressed markdown schema block, nothing else.

























