/**
 * FermiGuessr mark — simple lightbulb (lucide).
 */

import { Lightbulb, type LucideProps } from 'lucide-react';

export function FermiGuessrIcon(props: LucideProps) {
  return <Lightbulb {...props} />;
}
