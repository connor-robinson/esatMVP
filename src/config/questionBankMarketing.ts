/**
 * Marketing snapshot of `ai_generated_questions` row count.
 * Includes unapproved / pending / rejected and both ESAT + TMUA.
 * Update manually when the bank grows materially (no runtime DB lookup).
 */
export const QUESTION_BANK_TOTAL_COUNT = 2334;

/** ISO date the snapshot above was last verified against production. */
export const QUESTION_BANK_TOTAL_COUNT_AS_OF = "2026-07-17";
