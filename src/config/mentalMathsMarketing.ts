/**
 * Marketing snapshot for homepage copy (avoid importing the full drill config tree).
 * Keep in sync with MENTAL_MATHS_MODULE_COUNT / topic count in drillDisplayFolders.ts.
 */
export const MENTAL_MATHS_MODULE_COUNT_MARKETING = 90;

/** Unique live topic IDs across mental-maths trainer folders (excludes coming soon). */
export const MENTAL_MATHS_TOPIC_COUNT_MARKETING = 37;
